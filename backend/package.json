{
  "name": "product-quality-agent-backend",
  "version": "1.0.0",
  "description": "Validation pipeline + RAG knowledge base + agent for e-commerce product feed quality",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "mcp": "node src/mcp_server.js",
    "build-kb": "node scripts/build-kb.js",
    "test": "jest --coverage --reporters=default --reporters=jest-junit",
    "lint": "eslint src tests scripts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "cors": "^2.8.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
